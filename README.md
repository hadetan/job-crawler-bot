# job-crawler-bot
A bot that crawls through given url on google using google custom search api saves the urls on a csv then another crawler will come and visit each links and extract job links from their job listing pages (the urls are of their job listing pages).
