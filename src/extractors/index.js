const extractFromStructuredData = require('./structured-data');
const extractWithIntelligentAnalysis = require('./intelligent-analysis');

module.exports = {
  extractFromStructuredData,
  extractWithIntelligentAnalysis
};
